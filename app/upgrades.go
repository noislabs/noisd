package app

import (
	"github.com/cosmos/cosmos-sdk/types/module"
)

// RegisterUpgradeHandlers returns upgrade handlers
func (app *NoisApp) RegisterUpgradeHandlers(cfg module.Configurator) {
	// register future upgrades
}
